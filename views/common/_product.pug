section.product
  .container
    .product__subtitle.subtitle Kamiak studio products
    h2.product__title.title My personal brand
    ul.product__list
      each item in products
        li.product__item
          img(src=`${item.src}`).product__img
          .product__hover
            .product__description
              .product__name= item.name
              .product__price= `$ ${item.price}`